const expect = require('chai').expect

const Vector = require('../lib/vector')
const DataFrame = require('../lib/dataframe')
const v = Vector.of
const df = DataFrame.of

describe('DataFrame', () => {
    describe('#of()', () => {
        it('should create a new dataframe', () => {
            // todo...
        })
    })
})
